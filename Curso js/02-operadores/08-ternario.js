// Definicion de la edad que sera evaluada
const edad = 12;

//           expresion ? 'si es true'       : 'si es false'
let acceso = edad > 17 ? 'Permitit ingreso' : 'No puede ingresar';

// Mostrar en consola el resultado de la evaluacion
console.log(acceso)